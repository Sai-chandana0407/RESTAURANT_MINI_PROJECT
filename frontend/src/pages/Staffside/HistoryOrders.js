import React from "react";
import "./Orders.css"; // Importing the CSS file

const HistoryOrders = () => (
  <div className="history-container">
    <header className="history-header">STAFF HISTORY DISABLED</header>
    <div className="alert alert-warning mt-4">
      Staff history access is currently disabled. Please contact your administrator.
    </div>
  </div>
);

export default HistoryOrders;